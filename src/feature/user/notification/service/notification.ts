// import { apiGet } from "@/lib/api/client"
// import { API_PATHS } from "@/lib/api/paths"

//   export const getInbox = ({ page, size }: { page: number; size: number }) =>
//     apiGet(`/api/v1/notification/inbox?page=${page}&size=${size}`)

//   export const markRead = (id: string) =>
//     api.patch(`/api/v1/notification/inbox/${id}/read`)

//   export const markAllRead = () =>
//     api.put(`/api/v1/notification/inbox/read-all`)
